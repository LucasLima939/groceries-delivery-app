import 'package:flutter/material.dart';

class ConfirmAccount extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Confirme sua conta"),
    );
  }
}
